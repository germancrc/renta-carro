<div class="card mb-4">
  <img src="{{auto.imagen}}" class="card-img-top img-fluid" alt="{{auto.nombre}}" style="max-height: 400px; object-fit: cover;">
  <div class="card-body">
    <h2 class="card-title">{{auto.nombre}}</h2>
    <p class="card-text">{{auto.descripcion}}</p>
    <a href="/autos" class="btn btn-primary">Volver al listado</a>
  </div>
</div>
